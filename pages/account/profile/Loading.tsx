type Props = {};

const Loading = (props: Props) => {
	return (
		<section className="d-flex justify-content-center align-items-center" style={{ minHeight: '30dvh' }}>
			<p className="fs-4">Loading...</p>
		</section>
	);
};

export default Loading;
